# L-System 分形植物 2.0 版
### 1、第二阶段功能总结（Version 2.0）
本阶段在 1.0 核心分形逻辑基础上，完成**全功能交互面板与视觉增强**，程序从“静态演示”升级为**可交互、可调节、可美化**的完整图形工具，满足课程大作业创新与交互要求。
- **完整 UI 控制面板**：实现植物类型选择、迭代次数、旋转角度、枝条长度实时调节。
- **多预设分形模型**：内置经典植物、蕨类、树木三种 L-System 规则，一键切换形态。
- **动态参数响应**：滑动条/数值框修改后立即重绘，所见即所得。
- **视觉美化升级**：颜色渐变、随机角度扰动，形态更自然、更贴近真实植物。
- **实用功能扩展**：随机生成、动画生长、自定义颜色、保存 PNG 图片。
- **性能与安全加固**：限制最大迭代次数，自动缩放枝条长度，避免卡顿与崩溃。

---
### 2、技术实现原理
1. **Qt 界面与交互实现**
- 使用 `QHBoxLayout`/`QVBoxLayout` 搭建顶部控制面板，加入 `QComboBox`、`QSlider`、`QSpinBox`、`QPushButton` 等控件。
- 通过 **信号与槽（Signal & Slot）** 绑定控件变化事件，参数修改后调用 `update()` 触发重绘。

2. **分形绘制增强**
- 基于海龟绘图，用 `QStack` 保存/恢复坐标与角度状态，实现递归分支。
- 按绘制深度计算**颜色渐变**，从树干色过渡到梢色，层次更清晰。
- 对旋转角度加入±2°随机扰动，形成**随机自然形态**，避免机械对称。

3. **动画与保存实现**
- 使用 `QTimer` 定时驱动逐段绘制，实现“生长动画”效果。
- 通过 `QPixmap` + `render()` 截图，调用 `QFileDialog` 保存为 PNG 图片。
- 用 `QColorDialog` 提供颜色拾取器，支持自定义茎干颜色。

4. **健壮性优化**
- 限制迭代次数 1~7 次，防止字符串过长导致程序卡死。
- 枝条长度随迭代次数自动缩放，保证图形始终在窗口内完整显示。

---
### 3、下一阶段目标（Version 3.0）
为进一步提升作品完整度、趣味性与展示效果，冲刺高分与流量赛道，计划实现：
#### ① 更强视觉与场景化
- 背景切换（草地、星空、雪景），实现季节/主题风格变换。
- 枝条粗细渐变：主干更粗、分枝更细，增强立体感。
- 顶端添加花朵/果实点缀，提升美观度。

#### ② 更多交互与趣味功能
- 鼠标点击位置生成植物，支持多株同时绘制。
- 增加生长速度调节、暂停/继续动画控制。
- 加入背景音乐与生长音效，提升沉浸感。

#### ③ 作品展示与提交优化
- 完善程序图标、窗口标题、启动信息。
- 增加关于界面、作者信息、算法说明。
- 整理代码版本记录，完善实验报告与 B 站演示视频。

---
